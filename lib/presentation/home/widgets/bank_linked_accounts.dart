import 'package:flutter/material.dart';

class BankLinkedAccounts extends StatelessWidget {
  const BankLinkedAccounts({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}